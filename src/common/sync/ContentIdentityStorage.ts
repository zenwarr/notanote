import { StoragePath } from "../storage/StoragePath";
import { ContentIdentity } from "./ContentIdentity";


export class ContentIdentityStorage {
  get(path: StoragePath): ContentIdentity | undefined {
    return undefined;
  }
}
